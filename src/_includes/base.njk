<!DOCTYPE html>
<html lang="de" class="scroll-smooth">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <title>Bella Vista Restaurant - Gastronomie</title>
    <meta name="description" content="Traditionelle italienische Familienrezepte seit 3 Generationen mit frischen regionalen Zutaten in authentisch gemütlicher Atmosphäre">
    <meta name="keywords" content="Bella Vista Restaurant, Gastronomie, Authentische Gerichte und deren Geschichte, Familientradition und Rezepte, Frische regionale Zutaten und Lieferanten">
    <link rel="stylesheet" href="/assets/css/main.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;700&family=Playfair+Display:wght@400;700&display=swap" rel="stylesheet">
  </head>
  <body class="bg-background text-text font-primary min-h-screen flex flex-col">
    <header class="bg-white/90 backdrop-blur-lg sticky top-0 z-40 shadow-sm">
      <nav class="container mx-auto flex justify-between items-center py-4 px-4 lg:px-8">
        <a href="#hero" class="text-primary font-black text-2xl tracking-tight" data-editable="nav-brand" data-type="text">Bella Vista</a>
        <ul class="flex gap-4 sm:gap-8 text-base">
          <li><a href="#hero" onclick="document.getElementById('hero').scrollIntoView({behavior:'smooth'});return false;" class="hover:text-accent transition-colors" data-editable="nav-hero" data-type="text">Start</a></li>
          <li><a href="#services" onclick="document.getElementById('services').scrollIntoView({behavior:'smooth'});return false;" class="hover:text-accent transition-colors" data-editable="nav-services" data-type="text">Leistungen</a></li>
          <li><a href="#about" onclick="document.getElementById('about').scrollIntoView({behavior:'smooth'});return false;" class="hover:text-accent transition-colors" data-editable="nav-about" data-type="text">Über uns</a></li>
          <li><a href="#contact" onclick="document.getElementById('contact').scrollIntoView({behavior:'smooth'});return false;" class="hover:text-accent transition-colors" data-editable="nav-contact" data-type="text">Kontakt</a></li>
        </ul>
      </nav>
    </header>
    <main class="flex-1">{{ content | safe }}</main>
    <footer class="bg-neutral text-sm text-secondary py-8 mt-16">
      <div class="container mx-auto flex flex-col md:flex-row items-center justify-between gap-4 px-4">
        <div class="flex flex-col md:flex-row items-center gap-2 md:gap-8">
          <span data-editable="footer-copyright" data-type="text">&copy; 2025 Bella Vista Restaurant</span>
          <span class="hidden md:inline">|</span>
          <a href="/impressum" class="hover:underline" data-editable="footer-impressum" data-type="text">Impressum</a>
          <span class="hidden md:inline">|</span>
          <a href="/datenschutz" class="hover:underline" data-editable="footer-datenschutz" data-type="text">Datenschutz</a>
        </div>
      </div>
    </footer>
    <script src="/assets/js/main.js"></script>
    <script>
      // Editor Support (for content editing mode)
      if (window.location.search.includes('webseitengenie_editor=true')) {
        document.body.classList.add('editor-active');
      }
    </script>
  
<!-- Webseitengenie Editor Support -->
<script>
(function() {
  // Only activate in editor mode
  if (!window.location.search.includes('webseitengenie_editor=true')) return;
  
  // Store original content
  const originalContent = new Map();
  
  // Listen for editor messages
  window.addEventListener('message', function(event) {
    if (event.source !== window.parent) return;
    
    const { type, elementId, content } = event.data;
    
    if (type === 'update-preview') {
      const element = document.querySelector('[data-editable="' + elementId + '"]');
      if (!element) return;
      
      // Store original
      if (!originalContent.has(elementId)) {
        originalContent.set(elementId, element.innerHTML);
      }
      
      // Update with animation
      element.style.transition = 'all 0.3s ease';
      element.innerHTML = content;
      element.style.backgroundColor = '#fef3c7';
      
      setTimeout(() => {
        element.style.backgroundColor = '';
      }, 300);
    }
    
    if (type === 'restore-original') {
      originalContent.forEach((content, id) => {
        const el = document.querySelector('[data-editable="' + id + '"]');
        if (el) el.innerHTML = content;
      });
    }
  });
  
  // Signal ready
  window.parent.postMessage({ type: 'preview-ready' }, '*');
})();
</script>
</body>
</html>
